function fn() {

    return {
        name: "Olavo"
    };
}

//karate-config contains javascript function that returns a JSON object
//the key values defined in the JSON object are available to all feature files